<!DOCTYPE html>
<html>
  <head>
    <title>My App - Log in</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- W3.CSS (if not already in your layout) -->
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
     <style>
    .w3-display-middle {
      position: absolute;
      top: 44%;
      left: 50%;
      transform: translate(-50%, -50%);
      -ms-transform: translate(-50%, -50%);}
    </style>
  </head>

  <!-- Make the body a display container so we can center content vertically & horizontally -->
  <body class="w3-light-grey w3-display-container" style="height: 100vh;">

    <!-- Centered card for the login form -->
    <div class="w3-display-middle w3-card w3-white w3-padding" style="max-width: 400px; width: 90%;">
      <h2 class="w3-center">Log in</h2>

      <!-- Your existing Devise form code -->
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="w3-margin-bottom">
          <%= f.label :email %><br>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w3-input w3-border" %>
        </div>

        <div class="w3-margin-bottom">
          <%= f.label :password %><br>
          <%= f.password_field :password, autocomplete: "current-password", class: "w3-input w3-border" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="w3-margin-bottom">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>

        <div>
          <%= f.submit "Log in", class: "w3-button w3-black w3-block" %>
        </div>
      <% end %>

      <div class="w3-center" style="margin-top: 16px;">
        <%= render "devise/shared/links" %>
      </div>
    </div>

  </body>
</html>
